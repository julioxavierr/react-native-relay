import React, { Component } from 'react';
import { Text } from 'react-native';

export default class NewUser extends Component {
    render() {
        return (
            <Text>New user form</Text>
        );
    }
}